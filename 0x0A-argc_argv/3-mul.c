#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * @argc: input
 * @argv: input
 *
 * Return: 0 if true, else 1
 */
int main()
{
	int a, b, pro;

	scanf("%d %d", &a, &b);
	pro = a * b;
	printf("%d\n", pro);

	return (1);
}
