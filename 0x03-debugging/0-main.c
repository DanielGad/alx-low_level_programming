#include <stdio.h>
/**
 * main - Test if positive or negative
 *
 * Return: 0 (success)
 */
int main(void)
{
	int i;

	if (i > 0)
		printf("%d is positive\n");
	if (i < 0)
		printf("%d is negative\n");

	return (0);
}

